import { RollNumberValidator } from './roll-number-validator';

describe('RollNumberValidator', () => {
  it('should create an instance', () => {
    expect(new RollNumberValidator()).toBeTruthy();
  });
});
